<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8"/>
    <title>Proba</title>
</head>
<body>
    <center>
        <h1>Proba</h1>
        <p>{{ urls|length }} links</p>
    </center>
    <ol align="left">
        {% set counter = 0 -%}
        {% for url in urls -%}
            {% if counter % 3 == 0 -%}
                <li>This is a third list item<li>
            {% else -%}
                <li><a href="{{ url }}">{{ url }}</a></li>
            {% endif -%}
        {% set counter = counter + 1 -%}
        {% endfor -%}
    </ol>
</body>
</html>